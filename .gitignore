# --- 1. 虚拟环境与依赖 (Virtual Environment & Dependencies) ---
# 忽略整个虚拟环境文件夹，它应该在每台机器上重新创建。
venv/
.venv/

# --- 2. 密钥与环境变量 (Secrets & Environment Variables) ---
# 绝对禁止上传！包含数据库密码、SECRET_KEY等绝密信息。
.env
*.env

# --- 3. Django 核心忽略项 (Django Core Ignores) ---
# 本地开发数据库，不应纳入版本控制。
db.sqlite3
*.sqlite3

# 用户上传的文件，应使用云存储等方案，而不是Git。
media/

# 由 `python manage.py collectstatic` 命令自动生成的文件夹。
staticfiles/

# 项目运行时产生的日志文件。
*.log

# --- 4. Python 自动生成文件 (Python Generated Files) ---
# Python 编译的字节码。
__pycache__/
*.pyc

# --- 5. 测试与报告 (Testing & Reports) ---
# 测试覆盖率报告。
htmlcov/
.coverage
.coverage.*
.pytest_cache/

# --- 6. IDE 与编辑器配置 (IDE & Editor Config) ---
# 忽略特定于开发环境的编辑器设置。
.vscode/
.idea/
*.swp
*.swo

# --- 7. 操作系统生成文件 (OS Generated Files) ---
# 忽略macOS和Windows的系统文件。
.DS_Store
Thumbs.db
